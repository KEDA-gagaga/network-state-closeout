interface:
  display_name: "Initialize Network State"
  short_description: "Initialize private network state with consent-based discovery"
  default_prompt: "Use $initialize-network-state to create my private global network-state skill, explain the privacy boundary, and ask before collecting local network structure."
