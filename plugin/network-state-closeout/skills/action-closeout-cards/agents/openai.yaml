interface:
  display_name: "Action Closeout Cards"
  short_description: "生成状态卡、过程卡及可回写网络状态的结构化交接"
  default_prompt: "Use $action-closeout-cards to write separate status and process cards, validate their network-state handoff when relevant, and reconcile it through $network-state."
